import SwapVertIcon from "@mui/icons-material/SwapVert";

const Sort = () => {
  return (
    <SwapVertIcon sx={{ color: "#133E87", cursor: "pointer", fontSize: 35 }} />
  );
};

export default Sort;
